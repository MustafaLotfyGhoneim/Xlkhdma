@using Xlkdma.Models
@model List<Wait>
@{
    ViewBag.Title = "الطلبات المنتظرة";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<table class="table table-striped p-2">
    <tr>
        <th>الخدمة</th><th>التفاصيل</th><th>تاريخ الطلب</th><th>رقم  تليفون العميل</th><th>إسم العميل</th><th></th><th></th>
    </tr>
    @foreach(var item in Model)
    {
<tr>
    <td>@item.waitName</td>
    <td>@item.waitDesc</td>
    <td>@item.waitDate</td>
    <td>@item.Customer.customerPhone</td>
    <td>@item.Customer.customerName</td>
    <td><a href="/operation/confirmOrder/@item.waitId" class="btn btn-info">تأكيد</a></td>
    <td>
    @*@Html.ActionLink("Delete", "حذف", new { id = item.waitId, @class="btn btn-info" },
    new { onclick = "هل تريد حذف الخدمة؟" })
    </td>*@
    @*<td><a href="/operation/confirmOrder/@item.waitId" class="btn btn-info">حذف</a></td>*@


</tr>
    }
</table>
